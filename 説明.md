## MIDI
**MIDI**（Musical Instrument Digital Interface）はMIDI楽器で発生したイベントをMIDI楽器の間でメッセージとして伝えるための規格である。

この記事では以下の種類のメッセージを取り上げる。
- **ノートオン** - 鍵盤のキーが押されたことを表すメッセージ
- **ノートオフ** - 鍵盤のキーが離されたことを表すメッセージ

ノートオンとノートオフは以下の3つのパラメータを持つ。
- **チャンネル** - 楽器や声部を区別するための0から15の整数
- **ノートナンバー** - どのキーが押された／離されたかを表す0から127の整数
- **ベロシティ** - キーを押された／離された際の速さを表す0から127の整数

**SMF**（Standard MIDI File）はよくMIDIファイルと呼ばれるもので、イベントメッセージとそれが発生した時刻を記録するためのファイル形式である。エディタが多く出回っている。

## WAV
音は波形としてあらわすことができる。時間軸を一定時間ごとに区切ってその時点での波形の値を取得することをサンプリングという。サンプリングする周期の逆数を**サンプリング周波数**という。サンプリングで得た値を**波形サンプル**という。

**WAV**は波形サンプル列を記録するためのファイル形式の一種で、多くの環境で再生できる。

## システムの目的と構成
### 動作の流れ
1. SMFからMIDIメッセージを読みだす。
2. MIDIメッセージをもとに波形サンプル列を生成する。
3. 波形サンプル列を波形ファイルに出力する。

### ソース構成
C言語で書かれている。入出力部分と波形サンプル生成部分を分けた。
- #### main関数を含む入出力部分
  [./common/main.c](./common/main.c)
  
  SMFを入力とし、WAVファイルを出力とする。具体的な仕様はREADMEのほうに書く。

  鍵盤楽器として使うためにリアルタイムで入出力を行うバージョンも作ったが、ソースの紹介は省く。

- #### 波形サンプル生成部分のインターフェース
  [./common/io.h](./common/io.h)
  
  以下の関数の宣言を含む。
  - ノートオンやノートオフが起こった時にそのパラメータを入出力部分から受け取り内部状態を変更する関数
  - 新しい波形サンプルが必要になった時に入出力部分から呼び出されて内部状態に応じて波形サンプルを計算し返す関数

- #### 波形サンプル生成部分
  以下で何通りか紹介する。リストの操作（ノードの挿入／削除）にはmalloc/free関数に代わってより高速な自作ガベージコレクション関数を用いる。

## 音と波形
音は**波形**としてあらわすことができる。波形は時刻$`t`$から媒質の変位$`y`$への関数である。

音の重ね合わせは波形の足し合わせに対応するとされる（**重ね合わせの原理**）。

任意の音の波形は**サイン波**の和に分解することができる。

サイン波は以下のように表される。
```math
y=a\sin(ft+\phi)
```
$`a`$は**振幅**、$`f`$は**周波数**、$`\phi`$は**位相**と呼ばれるパラメータである。

## サイン波の干渉
2つの同じ周波数のサイン波を足し合わせるとひとつのサイン波になる。
サイン波の合成前の振幅がそれぞれ$`a_0, a_1`$であり、合成後の振幅が$`b`$であるとき、
- $`{a_0}^2+{a_1}^2\gt b^2`$のときは**弱め合う干渉**が
- $`{a_0}^2+{a_1}^2\lt b^2`$のときは**強め合う干渉**が

生じたという。

2個より多い数の同じ周波数のサイン波の足し合わせの場合にこれらの概念を拡張する。サイン波の合成前の振幅がそれぞれ$`a_0, a_2,\dots, a_{n-1}`$であり、合成後の振幅が$`b`$であるとき、
- $`\sum_{i=0}^{n-1}{a_i}^2\gt b^2`$のときは弱め合う干渉が
- $`\sum_{i=0}^{n-1}{a_i}^2\lt b^2`$のときは強め合う干渉が

生じたということにする。

そして、便宜的に$`\sum_{i=0}^{n-1}{a_i}^2=b^2`$のときは干渉が生じていないということにする。

## サイン波の干渉の生じていない合成波形
サイン波
```math
a_0 \cdot \sin(f_0 \cdot t),a_1 \cdot \sin(f_1 \cdot t), \cdots 
```
があったとする(話を簡単にするために初期位相をそろえてある。)。振幅や周波数は時間で変化しない。

これらを干渉が生じないように合成した波形として以下のものを定義する。(重ね合わせの原理には沿っていないことに注意。)
1. $`f_0=f_1=\cdots`$のときは
```math
\sqrt{{a_0}^2+{a_1}^2+\cdots}\cdot\sin(f_0\cdot t)
```
2. $`f_0,f_1,\cdots`$が互いに異なるときは
```math
a_0 \cdot \sin(f_0 \cdot t)+a_1 \cdot \sin(f_1 \cdot t)+ \cdots
```
3. それ以外のときは、まず同じ周波数のサイン波どうしで1.の波形を導き、それらに対して2.の波形を導く。

今後はこの波形を「干渉の生じていない合成波形」と呼ぶことにする。

### 実装
[./sin/sin.c](./sin/sin.c)

ノートオンが起こったときにひとつまたは複数のサイン波を発生させる。ノートオンごとに新たなIDを発行し、発生させたサイン波に紐づける。ノートオフが起こったときに対応するノートオンIDをもつサイン波をすべて削除する。

現在存在しているサイン波の周波数を小さい順にリストにして管理する。振幅は周波数ごとにやはりリストにして管理する。周波数リスト上に周波数$`f_0, f_1,\dots f_{n-1}`$があり、$`f_i`$の振幅リスト上に振幅$`a_{(i,0)}, a_{(i,1)},\dots a_{(i,m_i-1)}`$があるとき、出力波形$`y`$は
```math
y=\sum_{i=0}^{n-1}\sqrt{\sum_{j=0}^{m_i-1}{a_{(i,j)}}^2}\sin(f_it)
```
になる。

エイリアシングを避けるために、ナイキスト周波数（サンプリング周波数の半分）よりも大きな周波数をもつサイン波は発生させない。

音高を感じさせる音は周期的な波形で表すことができる。周期的な波形はその周波数の1倍、2倍、3倍...の周波数をもつサイン波の和に分解することができる。今回は、まずノートナンバーから周波数を決め、その周波数の1倍、2倍、3倍...の周波数をもつサイン波を発生させることにする。

受け取ったノートオンメッセージのパラメータから、発生させる各サイン波の振幅を決定する方法は無数にある。サイン波間の振幅の比率が音色を決める。様々な音色を試しやすいように、音色定義部分のソースを波形サンプル生成部分から分けた。今回は音色定義部分のソースとして以下のものを用意した。
1. **のこぎり波の再現**\
   [./sin/instruments/saw.c](./sin/instruments/saw.c)\
   出力例：[https://youtu.be/J-N0dU-0Slc](https://youtu.be/J-N0dU-0Slc)\
   のこぎり波を構成するサイン波の振幅はその周波数に反比例する。
3. **のこぎり波と矩形波の再現**\
   [./sin/instruments/saw_squ.c](./sin/instruments/saw_squ.c)\
   出力例：[https://youtu.be/4I6i4BG9YC0](https://youtu.be/4I6i4BG9YC0)\
   矩形波はのこぎり波から偶数番目のサイン波を抜いたものである。チャンネル番号が奇数のときに矩形波が鳴るようにした。
5. **人間の母音"a"の再現**\
   [./sin/instruments/vowels.c](./sin/instruments/vowels.c)\
   出力例：[https://youtu.be/_jCuZE0prwE](https://youtu.be/_jCuZE0prwE)\
   のこぎり波にあるフィルタをかけて人間の声に近づけた。偶数番チェンネルが女声で、奇数番チャンネルが男声。

実装の観点からは、のこぎり波や矩形波は複数のサイン波の和として合成するよりも時刻から直接計算したほうが効率が良い。以下では、のこぎり波の「干渉の生じていない」合成波形をサイン波を経由せずにのこぎり波の和として得る方法を紹介する。

## のこぎり波のフーリエ級数展開
のこぎり波$`\mathrm{saw}(x)`$を区間$`0\le x\lt 2\pi`$では
```math
\mathrm{saw}(x)=\frac{\pi-x}{2}
```
と定義し、ほかの区間ではこれを周期的に延長したものとして定義すると、
$`\mathrm{saw}(x)`$は次のフーリエ級数をもつ。
```math
\mathrm{saw}(x)
=
\sum_{k=1}^{\infty}\frac{1}{k}\sin(k\cdot x)
```

## のこぎり波の干渉の生じていない合成波形
のこぎり波どうしを整数比の周波数比で重ね合わせると同じ周波数のサイン波の重なりが発生する。これが干渉を発生させる。任意の複数ののこぎり波のすべてに共通して存在する同じ周波数のサイン波の、単なる足し合わせと干渉の生じない合成波形のずれ全体はのこぎり波になる。このことを利用してのこぎり波の干渉の生じていない合成波形をあるのこぎり波の和として得る。

$`n`$個ののこぎり波
```math
a_0\cdot\mathrm{saw}(f_0\cdot t),\dots,
a_i\cdot\mathrm{saw}(f_i\cdot t),\dots,
a_{n-1}\cdot\mathrm{saw}(f_{n-1}\cdot t)
```
があったとする。(これらののこぎり波を**仮音**と呼ぶことにする)

仮音の添字集合
```math
I=\{0,1,\dots,n-1\}
```
を定義しておく。

各$`i\in I`$に対して$`f_i`$は正の整数であるとする。

すべての仮音をいったんフーリエ級数展開でサイン波に分解したとき、それらサイン波の干渉の生じていない合成波形を、仮音の「干渉の生じていない合成波形」と呼ぶことにする。

各$`J\subseteq I`$に対してのこぎり波
```math
b_J\cdot\mathrm{saw}(g_J\cdot t)
```
を与える。(**実音**と呼ぶことにする)

各$`J\subseteq I`$に対して$`g_J`$は正の整数であるとする。

実音の和
```math
\sum_{J \subseteq I}b_J\cdot\mathrm{saw}(g_J\cdot t)
```
が、仮音の干渉の生じていない合成波形に等しくなるように各$`b_J`$と$`g_J`$を決める方法には以下のようなものがある。

### 実音の周波数
$`g_J`$は$`J=\emptyset`$ならば$`1`$、そうでないならば$`\{f_i|i\in J\}`$の最小公倍数であるとする。

### 実音の振幅
$`J\subseteq I`$とする。

$`i\in J`$であるようなすべての仮音$`a_i\cdot\mathrm{saw}(f_i\cdot t)`$に共通して存在する同じ周波数のサイン波の合成波形は
```math
\sum_{k=1}^{\infty}\sqrt{\sum_{i\in J}\Bigl(\frac{a_i\cdot f_i}{k\cdot g_J}\Bigr)^2}\cdot\sin(k\cdot g_J\cdot t)
=
\sqrt{\sum_{i\in J}\Bigl(\frac{a_i\cdot f_i}{g_J}\Bigr)^2}\cdot\mathrm{saw}(g_J\cdot t)
```
である。

一方、$`J'\subseteq J`$であるようなすべての実音$`b_{J'}\cdot\mathrm{saw}(g_{J'}\cdot t)`$に共通して存在する同じ周波数のサイン波の和は
```math
\sum_{k=1}^{\infty}\sum_{J' \subset J}\frac{b_{J'}\cdot g_{J'}}{k\cdot g_J}\cdot\sin(k\cdot g_J\cdot t)
=
\sum_{J' \subset J}\frac{b_{J'}\cdot g_{J'}}{g_J}\cdot\mathrm{saw}(g_J\cdot t)
```
である。

両者を等しくするには、
```math
\sqrt{\sum_{i\in J}\Bigl(\frac{a_i\cdot f_i}{g_J}\Bigr)^2}
=
\sum_{J' \subset J}\frac{b_{J'}\cdot g_{J'}}{g_J}
```
```math
\sqrt{\sum_{i\in J}(a_i\cdot f_i)^2}
=
\sum_{J' \subset J}b_{J'}\cdot g_{J'}
```
でなければならない。

$`b_J`$を決定するには$`J'\subset J`$であるようなすべての$`b_{J'}`$がすでに決定さている必要がある。

### 実装
[./saw_squ/saw.c](./saw_squ/saw.c)

集合演算はビット演算で実現する。$`I`$の部分集合に番号(実音番号と呼ぶことにする)を振る。$`I`$の部分集合$`J`$に$`i`$が含まれるとき、$`J`$に与えられた番号$`j`$の第$`i`$ビットが`1`になり、含まれないときは`0`になるようにする。

仮音と実音はリストで管理する。仮音や実音の番号はそれが属するリスト上の位置(先頭から何番目か)で表現する。ノードの挿入や削除により自分の位置が変わると仮音番号や実音番号がずれるが、それらが満たさなければならない関係は維持される。

出力例はほぼ同じ音なので省略する。

## 矩形波のフーリエ級数展開
矩形波$`\mathrm{sqw}(x)`$を区間$`0\le x\lt \pi`$では
```math
\mathrm{sqw}(x)=-\frac{\pi}{4}
```
区間$`\pi \le x\lt 2\pi`$では
```math
\mathrm{sqw}(x)=\frac{\pi}{4}
```
と定義し、ほかの区間ではこれを周期的に延長したものとして定義すると、
$`\mathrm{sqw}(x)`$は次のフーリエ級数をもつ。
```math
\mathrm{sqw}(x)
=
\sum_{k=1}^{\infty}\frac{1}{2k-1}\sin((2k-1)\cdot x)
```
矩形波はのこぎり波から偶数次のサイン波を取り除いたものであるといえる。

## のこぎり波と矩形波の干渉が生じない重ね合わせ
$`J\subseteq I`$とする。$`i\in J`$を番号としてもつような仮音の一部が矩形波の場合も、先ほどの実音の周波数と振幅をそのまま使って干渉しない重ね合わせを定義することができる。しかし次のことに注意が必要である。
- $`J`$に対応する実音の波形は矩形波である。
- 矩形波の仮音の周波数どうしの比が奇数対奇数でない場合や、 矩形波の仮音の周波数とのこぎり波の仮音の周波数の比が偶数対奇数でない場合は$`J`$に対応する実音の振幅は$`0`$である。

仮音(のこぎり波または矩形波)が互いに干渉する組み合わせかどうかを判断しやすいように、各仮音にある整数(**ランク**と呼ぶことにする)を与える。

その仮音の周波数$`f_i`$を素因数分解したときの$`2`$の個数が$`p`$だとしたとき、その仮音が矩形波だったら$`p`$ビットが$`1`$でそれ以外は$`0`$、のこぎり波だったら$`p`$ビットとそれより上のビットがすべて$`1`$でそれ以外は$`0`$になるように仮音ランクを定める。

仮音どうしが干渉する組み合わせかどうか判断するには仮音それぞれがもつランクどうしをビット乗算して、そのビット積が$`0`$になったら干渉しない、$`0`$以外になったら干渉するとすればよい。またそのビット積があるビットより上がすべて$`1`$だったらその仮音の組み合わせに対応する実音はのこぎり波であり、そうでなければ矩形波であるとすればよい。このビット積を対応する実音のランクとする。また実音のランクが$`0`$になったらその実音の振幅は$`0`$であるとする。

### 実装
[./saw_squ/saw_squ.c](./saw_squ/saw_squ.c)

先ほどののこぎり波どうしの干渉の生じない合成の実装を変更する形で実装する。

## のこぎり波や矩形波を直接合成する方法のサイン波から合成する方法に対する利点と欠点
### 利点
- ある仮音の数まではより少ない数の波形で合成波形を実現することができる。
- のこぎり波や矩形波ひとつあたりを生成するための計算量はサイン波の和として生成するよりも小さい。

### 欠点
- サイン波から合成する方法では発生させなければならないサイン波の個数は鳴らしたいのこぎり波の個数に比例するが、実音の個数は仮音の個数の2のべきに比例するので、仮音がある個数を超えると計算効率の優位が失われる。
- サイン波からの合成する場合は最初からナイキスト周波数以上のサイン波を入れないように合成することができるが、直接合成されたのこぎり波はエイリアシングを含んでおり対策が必要である。
